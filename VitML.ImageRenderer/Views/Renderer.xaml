<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VitML.ImageRenderer.Views" x:Class="VitML.ImageRenderer.Views.Renderer" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Background="Black">
        <Border BorderThickness="0" BorderBrush="White">
        <Grid>
        <Image Name="image1" HorizontalAlignment="Stretch" Height="Auto" Margin="0" 
                         VerticalAlignment="Stretch" Width="Auto"/>
        <Grid Name="fpsView" Background="LightGray" Width="Auto" Height="Auto" HorizontalAlignment="Left" VerticalAlignment="Top" 
			Opacity="0.5" Margin="5">
            <Label x:Name="fpsLbl" Content="150" HorizontalContentAlignment="Center" VerticalAlignment="Center" 
				FontWeight="Bold" FontSize="24" Margin="5,0,5,0" />
        </Grid>
        </Grid>
        </Border>
    </Grid>
</UserControl>
